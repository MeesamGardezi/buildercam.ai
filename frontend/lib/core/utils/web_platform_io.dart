// Purpose: Native (non-web) implementation of the web platform helpers.

/// Whether the app is running inside a mobile web browser.
/// Always `false` on native platforms.
bool get isMobileWeb => false;
